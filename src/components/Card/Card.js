import './Card.scss';
import React from 'react';
import {Component} from 'react';

class Card extends Component {
    render() {
        return (
            <div className="card">
                Card
            </div>
        );
    }
}

export default Card;
